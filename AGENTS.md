# queued-dictation

This repository is in the specification stage. Use GitHub Issues for the decision map and work tracking.

## Agent skills

### Issue tracker

Track issues and specs in this repository's GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical triage label names. See `docs/agents/triage-labels.md`.

### Domain docs

Use a single-context layout with a root `CONTEXT.md` and `docs/adr/`, created as decisions require. See `docs/agents/domain.md`.
